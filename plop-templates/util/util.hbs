/**
 * @packageDocumentation
 * @module @tomato-node/{{scope}}
 */
/**
 * 函数描述
 *
 * 新增于v{{version}}
 *
 * 脚本举例
 * ```
 *   import { {{name}} } from '@tomato-node/{{scope}}'
 * ```
 */
export function {{name}}() {
  
}
